
    var inicio = 2 
    var fim = 20 
    var pas = 3
    var soma = inicio + pas
    
    
    while (fim > soma) {
        console.log(`o valor de inicio é ${inicio}`)
        console.log(`o valor final é ${fim}`)
        console.log(`o valor de cada passo é de ${pas}`)
        soma + pas
        console.log('o resultado é:') + soma
    }
